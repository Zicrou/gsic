<nav class="navbar navbar-toggleable-md navbar-inverse fixed-top bg-inverse">
  <button class="navbar-toggler navbar-toggler-right hidden-lg-up" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <% if !current_user.is_a?(GuestUser) %>
  <% if logged_in?(:user) %>
    <span class="" style="color:white;">||<%= current_user.role.upcase %></span>
  <% end %>
    <span class=""><%=link_to "|Dashboard(AESC)", etudiants_path, class:"navbar-brand"%></span>
    
  <% elsif %>
    <span class="navbar-brand">AESC</span>
  <% end %>
  
  <div class="collapse navbar-collapse" id="navbarsExampleDefault">
    <ul class="navbar-nav mr-auto">
      
      <li class="nav-item">
        <span class="style-color"><%#= login_helper "nav-link"%></span>
      </li>
    <% if current_user.is_a?(GuestUser) %>
      <div class="nav-item">
       <span class="style-color"><%= link_to "Se Connecter", new_user_session_path, class:"nav-link span" %></span>
        <%#= link_to "Configurer ma zone", zones_path, class:"nav-link span" if logged_in?(:site_admin, :president, :responsable_zone, :embassade) %>
      </div>
      <div class="nav-item">
       <span class="style-color"><%= link_to "S'inscrire", new_user_registration_path, class:"nav-link span" %></span>
        <%#= link_to "Configurer ma zone", zones_path, class:"nav-link span" if logged_in?(:site_admin, :president, :responsable_zone, :embassade) %>
      </div>
    <% elsif current_user.role == :user %>
      <div class="nav-item">
       <span class="style-color"><%= link_to "Déconnexion",destroy_user_session_path,method: :delete, class:"nav-link span" %></span>
        <%#= link_to "Configurer ma zone", zones_path, class:"nav-link span" if logged_in?(:site_admin, :president, :responsable_zone, :embassade) %>
      </div>
      <div class="nav-item">
       <span class="style-color"><%= link_to "M'enregistrer", new_etudiant_path, class:"nav-link span" %></span>
        <%#= link_to "Configurer ma zone", zones_path, class:"nav-link span" if logged_in?(:site_admin, :president, :responsable_zone, :embassade) %>
      </div>
    <% elsif current_user.role == :site_admin or current_user.role == :president or current_user.role == :responsable_zone %>
      <div class="nav-item">
       <span class="style-color"><%= link_to "Déconnexion",destroy_user_session_path,method: :delete, class:"nav-link span" %></span>
        <%#= link_to "Configurer ma zone", zones_path, class:"nav-link span" if logged_in?(:site_admin, :president, :responsable_zone, :embassade) %>
      </div>
      <div class="nav-item">
       <span class="style-color"><%= link_to "M'enregistrer", new_etudiant_path, class:"nav-link span" %></span>
        <%#= link_to "Configurer ma zone", zones_path, class:"nav-link span" if logged_in?(:site_admin, :president, :responsable_zone, :embassade) %>
      </div>
      <div class="nav-item">
       <span class="style-color"><%= link_to "Ma Zone", root_path() + "etudiants/mazone", class:"nav-link span" %></span>
        <%#= link_to "Configurer ma zone", zones_path, class:"nav-link span" if logged_in?(:site_admin, :president, :responsable_zone, :embassade) %>
      </div>
      <div class="nav-item">
       <span class="style-color"><%= link_to "Configurer ma zone", zones_path, class:"nav-link span" %></span>
        <%#= link_to "Configurer ma zone", zones_path, class:"nav-link span" if logged_in?(:site_admin, :president, :responsable_zone, :embassade) %>
      </div>
    <% end %>
      

      <!--li class="nav-item">
        <a class="nav-link" href="#">Profile</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Help</a>
      </li-->
    </ul>
    <!--form class="form-inline mt-2 mt-md-0">
      <input class="form-control mr-sm-2" type="text" placeholder="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form-->
  </div>
</nav>

