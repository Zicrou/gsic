<main class="col-sm-9 offset-sm-3 col-md-10 offset-md-2 pt-3">
  <%= form_with(model: carteconsulaire, local: true) do |form| %>
    <% if carteconsulaire.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(carteconsulaire.errors.count, "error") %> prohibited this carteconsulaire from being saved:</h2>

        <ul>
        <% carteconsulaire.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</main>